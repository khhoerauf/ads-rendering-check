{
	"devDependencies": {
		"cypress": "^14.0.0",
		"prettier": "^3.3.3"
	},
	"scripts": {
		"cy:open": "cypress open",
		"cy:run": "cypress run --headless",
		"cy:ci": "npm run format && npm run cy:run",
		"format": "prettier --write ."
	}
}
